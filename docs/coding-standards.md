# Coding Standards

- Use strict TypeScript and typed props for every component.
- Favor server components and keep client components scoped to interaction points.
- Avoid unsafe HTML rendering and inline JavaScript.
- Prefer small, focused, composable building blocks over large monolithic components.
- Reuse tokens, layout primitives, and the shared UI layer consistently.
